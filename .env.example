# AI Product Factory — environment placeholders (M1 skeleton)
# Copy to .env and fill values when later milestones need them.
# Never commit real secrets.

NODE_ENV=development
PORT=3000

# Future: ResearchProvider / TextProvider / ImageProvider / Marketplace / Storage
# RESEARCH_PROVIDER=
# TEXT_PROVIDER_API_KEY=
# IMAGE_PROVIDER_API_KEY=
# MARKETPLACE_API_KEY=
# STORAGE_PATH=./.data
