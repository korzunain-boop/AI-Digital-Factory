/**
 * Dashboard entry — M1 skeleton placeholder.
 * No UI features yet (operations dashboard lands in M8).
 */
function main(): void {
  console.log('[dashboard] skeleton placeholder (milestone M1)');
}

main();
